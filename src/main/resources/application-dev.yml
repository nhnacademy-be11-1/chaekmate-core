spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://s4.java21.net:13306/project_be11_team1_dev?characterEncoding=UTF-8
    username: project_be11_team1
    password: OvE[4FtgeJLi!ibR

  jpa:
    hibernate:
      ddl-auto: none # validate 로 수정 (전체 entity  수정 후)
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        highlight_sql: true
        auto_quote_keyword: true
    open-in-view: false

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

server:
  port: 8081
